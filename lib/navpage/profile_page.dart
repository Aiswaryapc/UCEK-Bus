import 'package:flutter/material.dart';

class proflie_page extends StatelessWidget {
  const proflie_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("ha..haaa..")),
    );
  }
}
